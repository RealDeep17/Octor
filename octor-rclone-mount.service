[Unit]
Description=Octor Rclone Google Drive Union Mount
After=network.target
Before=docker.service

[Service]
Type=simple
User=ubuntu
Group=ubuntu
ExecStartPre=/usr/bin/mkdir -p /srv/octor/infra-data/drive-mount
ExecStart=/usr/bin/rclone --config /home/ubuntu/.config/rclone/rclone.conf mount ALPHA_UNION: /srv/octor/infra-data/drive-mount \
    --vfs-cache-mode off \
    --buffer-size 128M \
    --drive-chunk-size 256M \
    --transfers 8 \
    --allow-other \
    --umask 000
ExecStop=/usr/bin/fusermount -uz /srv/octor/infra-data/drive-mount
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
